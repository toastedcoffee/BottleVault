plugins {
    // Pinned: 1.0.0 (proposed by dependabot 2026-04-22) does not resolve on the Gradle Plugin Portal.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.10.0"
}

rootProject.name = "bottlevault"
